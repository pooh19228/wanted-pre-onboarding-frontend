import React from 'react';
import Login from '../../component/unit/login/login.index';

const LoginPage = () => {
    return (
        <div>
            <Login/>
        </div>
    );
};

export default LoginPage;