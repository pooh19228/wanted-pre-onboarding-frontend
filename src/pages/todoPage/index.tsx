import React from 'react';
import TodoList from '../../component/unit/todo/todo.index';

const TodoPage = () => {
    return (
        <div>
            <TodoList/>
        </div>
    );
};

export default TodoPage;